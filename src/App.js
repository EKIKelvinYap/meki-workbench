import React from 'react';
import './App.scss';
import NavigatorBar from './nav/NavigatorBar';

const App = () => {
    return (
        <div className="App">
            <NavigatorBar />
        </div>
    );
}

export default (App);
